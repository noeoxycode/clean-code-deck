# Tomcat Server Configuration
server:
  address: 0.0.0.0
  port: 8080
  compression:
    enabled: true


# MongoDB configuration
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/clean-code-deck
      database: clean-code-deck
      username: root
      password: root